# Patch Management

## AWS

Use `sudo yum update -y` for Amazon Linux.

## Azure and GCP

Use `sudo apt update && sudo apt upgrade -y` for Ubuntu/Debian servers.

## Recommendation

Patch regularly and validate Apache service health after updates.
